// Auto-generated by oag — do not edit
import { describe, it, expect, vi } from "vitest";
import { ApiClient } from "./client";
import type { ClientConfig } from "./client";

import type { CreateChatCompletionBody, CreateChatCompletionEventVariant1, CreateChatCompletionEventVariant2, CreateChatCompletionResponse, CreateChatCompletionStreamBody, CreateChatCompletionStreamEventVariant1, CreateChatCompletionStreamEventVariant2, GetModelResponse, ListModelsResponse, SubmitFeedbackBody } from "./types";


function createMockFetch(status = 200, body: unknown = {}) {
  return vi.fn().mockResolvedValue({
    ok: status >= 200 && status < 300,
    status,
    statusText: status === 200 ? "OK" : "Error",
    json: () => Promise.resolve(body),
    text: () => Promise.resolve(JSON.stringify(body)),
    body: null,
  } as unknown as Response);
}

function createClient(fetchFn?: typeof globalThis.fetch): ApiClient {
  const config: ClientConfig = {
    baseUrl: "https://api.test.com",
    fetch: fetchFn ?? createMockFetch(),
  };
  return new ApiClient(config);
}

describe("ApiClient", () => {
  it("can be instantiated with config", () => {
    const client = createClient();
    expect(client).toBeInstanceOf(ApiClient);
  });

  it("can be instantiated with custom headers", () => {
    const config: ClientConfig = {
      baseUrl: "https://api.test.com",
      headers: { Authorization: "Bearer token" },
      fetch: createMockFetch(),
    };
    const client = new ApiClient(config);
    expect(client).toBeInstanceOf(ApiClient);
  });

  it("can be instantiated with custom fetch", () => {
    const mockFetch = createMockFetch();
    const client = createClient(mockFetch);
    expect(client).toBeInstanceOf(ApiClient);
  });



  describe("listModels", () => {
    it("method exists", () => {
      const client = createClient();
      expect(typeof client.listModels).toBe("function");
    });

    it("makes GET request to correct URL", async () => {
      const mockFetch = createMockFetch(200, {} as ListModelsResponse);
      const client = createClient(mockFetch);
      await client.listModels();
      expect(mockFetch).toHaveBeenCalledTimes(1);
      const [url, init] = mockFetch.mock.calls[0];
      expect(url).toContain("/models");
      expect(init.method).toBe("GET");
    });


    it("throws on non-OK response", async () => {
      const mockFetch = createMockFetch(500);
      const client = createClient(mockFetch);
      await expect(client.listModels()).rejects.toThrow();
    });
  });




  describe("getModel", () => {
    it("method exists", () => {
      const client = createClient();
      expect(typeof client.getModel).toBe("function");
    });

    it("makes GET request to correct URL", async () => {
      const mockFetch = createMockFetch(200, {} as GetModelResponse);
      const client = createClient(mockFetch);
      await client.getModel("test");
      expect(mockFetch).toHaveBeenCalledTimes(1);
      const [url, init] = mockFetch.mock.calls[0];
      expect(url).toContain("/models/test");
      expect(init.method).toBe("GET");
    });


    it("throws on non-OK response", async () => {
      const mockFetch = createMockFetch(500);
      const client = createClient(mockFetch);
      await expect(client.getModel("test")).rejects.toThrow();
    });
  });




  describe("createChatCompletionStream", () => {
    it("method exists", () => {
      const client = createClient();
      expect(typeof client.createChatCompletionStream).toBe("function");
    });

    it("returns an async iterable", () => {
      const client = createClient();
      const result = client.createChatCompletionStream({} as CreateChatCompletionBody);
      expect(result).toBeDefined();
      expect(typeof result[Symbol.asyncIterator]).toBe("function");
    });
  });




  describe("createChatCompletion", () => {
    it("method exists", () => {
      const client = createClient();
      expect(typeof client.createChatCompletion).toBe("function");
    });

    it("makes POST request to correct URL", async () => {
      const mockFetch = createMockFetch(200, {} as CreateChatCompletionResponse);
      const client = createClient(mockFetch);
      await client.createChatCompletion({} as CreateChatCompletionBody);
      expect(mockFetch).toHaveBeenCalledTimes(1);
      const [url, init] = mockFetch.mock.calls[0];
      expect(url).toContain("/chat/completions");
      expect(init.method).toBe("POST");
    });


    it("sends request body", async () => {
      const mockFetch = createMockFetch(200, {} as CreateChatCompletionResponse);
      const client = createClient(mockFetch);
      await client.createChatCompletion({} as CreateChatCompletionBody);
      const [, init] = mockFetch.mock.calls[0];
      expect(init.body).toBeDefined();
    });


    it("throws on non-OK response", async () => {
      const mockFetch = createMockFetch(500);
      const client = createClient(mockFetch);
      await expect(client.createChatCompletion({} as CreateChatCompletionBody)).rejects.toThrow();
    });
  });




  describe("createChatCompletionStream", () => {
    it("method exists", () => {
      const client = createClient();
      expect(typeof client.createChatCompletionStream).toBe("function");
    });

    it("returns an async iterable", () => {
      const client = createClient();
      const result = client.createChatCompletionStream({} as CreateChatCompletionStreamBody);
      expect(result).toBeDefined();
      expect(typeof result[Symbol.asyncIterator]).toBe("function");
    });
  });




  describe("submitFeedback", () => {
    it("method exists", () => {
      const client = createClient();
      expect(typeof client.submitFeedback).toBe("function");
    });

    it("makes POST request to correct URL", async () => {
      const mockFetch = createMockFetch(204);
      const client = createClient(mockFetch);
      await client.submitFeedback({} as SubmitFeedbackBody);
      expect(mockFetch).toHaveBeenCalledTimes(1);
      const [url, init] = mockFetch.mock.calls[0];
      expect(url).toContain("/chat/feedback");
      expect(init.method).toBe("POST");
    });

    it("returns undefined on 204", async () => {
      const mockFetch = createMockFetch(204);
      const client = createClient(mockFetch);
      const result = await client.submitFeedback({} as SubmitFeedbackBody);
      expect(result).toBeUndefined();
    });
  });


});